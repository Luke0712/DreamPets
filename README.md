# DreamPets

一个最小 Electron 桌面宠物。当前默认站立形象使用：

`/Users/luke/Luke/Pets/assets/panda-breath-blink.webp`

`/Users/luke/Pictures/QQpets/Adult/Enter1/Enter1.gif` 已保留为入场动画素材路径。

## 使用

```bash
npm start
```

- 拖动宠物本体可以移动位置。
- 右键宠物可以变大、变小或退出。
- 更换形象时修改 `src/petConfig.js` 里的 `imagePath`。
